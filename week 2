



week2 prompt
我正在做一個專案：

Cloud-Native Financial Transaction Platform on Kubernetes

目前進度（Week 1 已完成）：

- Spring Boot transaction API
- create transaction（含 idempotency key）
- query transaction API
- MySQL schema（transactions table）
- TransactionStatus enum（含狀態流轉）
- optimistic locking（@Version）
- Spring Boot Actuator（readiness / liveness）
- Dockerfile 已完成（可 container run）

技術棧：
- Java / Spring Boot
- MySQL
- Docker
- 之後會上 AWS EKS + GitHub Actions

現在我要開始 Week 2：

🎯 目標：
把系統升級成「金融系統」，加入 batch settlement

我要你幫我一步一步完成：

1️⃣ 設計 settlement 相關 DB schema（像銀行結算系統）
2️⃣ 設計每日結算 batch flow（包含 failure scenario）
3️⃣ 設計 idempotent batch（避免 double count）
4️⃣ 設計 single-writer 機制（避免多 pod 同時跑）
5️⃣ 實作 Spring Boot batch job（可以用 @Scheduled 或其他方式）
6️⃣ 模擬 batch crash 並設計 recovery
7. 升級成kafka - consumer 要 Manual Ack
8. 實作 DLQ (Dead Letter Queue)
9. multi thread







Happy Path（正常流程）

[1] 建立 batch（PENDING）

[2] 更新 batch → PROCESSING

[3] 撈 transaction（未結算）

[4] 寫入 settlement_item（PENDING）

[5] 一筆一筆處理：
      → SUCCESS / FAILED

[6] 聚合結果：
      → total_amount
      → total_count

[7] 更新 batch → COMPLETED




多個 pod 的方式 - 用db 去擋

如果拿到鎖 但是最後掛掉 - 用 Timeout 機制


個別取鎖看看 在 SettlementBatchRepository.tryLockBatch

然後在service 裡面 取 lock





[Kubernetes]

Deployment (replicas=3)
        ↓
Spring Boot Pods (3個)
        ↓
        → MySQL（單一 DB）



runBatch()
  ↓
insert items
  ↓
process items
  ↓
COMPLETED



runBatch（不加 @Transactional）
   ↓
tryLock（單獨 transaction）
   ↓
insert items（transaction）
   ↓
processItems（每筆 / 每批 transaction）




--------- Kafka 版本


Transaction → (DB)
              ↓
        Outbox Table
              ↓
     Outbox Publisher（Kafka Producer）
              ↓
        Kafka Topic（settlement-events）
              ↓
   Settlement Worker（Consumer group）



Outbox Pattern 「不要直接寄信，先把信放進發信匣。」

DB transaction（atomic）
  ↓
transaction table
outbox table（event）


測試網址
curl -X POST http://localhost:8080/batch/runKafka






Summary:

ZooKeeper 是 kafka 要用到的東西
用docker compose 就要用到


用OutboxPublisher.java
為一隻背景批次 它只抓狀態為 NEW 的資料，丟給 Kafka。
丟一個訊息到DB 說有資料在 kafka
丟一些訊息到 kafka
用SchedulerLock -> DB鎖住防止同時執行


為什麼不直接在 Service 裡發 Kafka？
會遇到「分散式事務」問題： 問題就是 kafka 無法下載一個 transaction 裡面

    情境 A： 你先發了 Kafka，結果資料庫 commit 失敗回滾了。
        結果： 消費端收到了訊息開始結算，但資料庫根本沒有這筆交易。（帳務不一致！）

    情境 B： 你先存資料庫成功了，準備發 Kafka 時網路斷了。
        結果： 資料庫狀態改好了，但 Kafka 沒訊息，這筆結算就永遠「卡死」在那裡沒人處理。（掉單！）



Service
SettlementKafkaServiceImpl.java
被觸發的批次
會去掃DB看有沒有未處理的item
會存訊息到DB 說有新的未處理的狀態



SettlementConsumer.java
背景批次
就是去kafka 裡面掃看有沒有未處理的資料
目前還有問題 就是 SettlementConsumer 執行一半掛掉 資料會消失



CommandLineRunner (testRun): 這是 Spring Boot 提供的一個鉤子，會在 Spring Context 完全啟動後、應用程式對外服務前執行。



SettlementConsumerMultiThread.java

用 多個 thread 去執行批次


