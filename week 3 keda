



week3 prompt
我正在做一個專案：

Cloud-Native Financial Transaction Platform on Kubernetes

目前進度（Week 1 已完成）：

- Spring Boot transaction API
- create transaction（含 idempotency key）
- query transaction API
- MySQL schema（transactions table）
- TransactionStatus enum（含狀態流轉）
- optimistic locking（@Version）
- Spring Boot Actuator（readiness / liveness）
- Dockerfile 已完成（可 container run）

技術棧：
- Java / Spring Boot
- MySQL
- Docker
- 之後會上 AWS EKS + GitHub Actions

現在我要開始 Week 3:

🎯 目標：
把系統升級成「金融系統」，加入 batch settlement

我要你幫我一步一步完成：

1. 試看看HPA 地端 k8s    local 會把 cpu 操爆 先放棄
2. keda (k8s event-driven autoscling)






Summary:

-- Kafka 的 Topic 像是永不回頭的輸送帶，訊息編號（Offset）是一直往後累加的，永遠不會倒退。


keda (k8s event-driven autoscling)

keda 為 k8s 的 plugin 安裝完後 重開機就會自動起來


0. deployment 跟 ScaledObject 這個重開機 重啟 k8s 都還會在

1. 增加一個 k8s ScaledObject, 他會去抓 設定的 deployment 然後

用kafka 當作event 去驅動 pod 的數目

2. yaml 裡面有 consumerGroup: financial-group1

要跟程式一樣

@KafkaListener(topics = "settlement-topic3", groupId = "financial-group1", concurrency = "3"

concurrency = 3 => 單一個 Pod 內部 開啟 3 個執行緒（Thread）
建議在有 KEDA 的情況下，concurrency 設為 1 即可，讓 KEDA 控管 Pod 數就好。

3. KEDA 的運作原理是去修改 Deployment 的 spec.replicas。
刪掉 ScaledObject ，不會在臨走前幫你把數量改回 0。

4. 有意外的產生三個 pod (在UI 按下 clear message)

因為 kafka 從 Offset 18, 但是執行 Clear Messages，Kafka 的 First Offset 已經跳到了 24
只要 Lag 大於你設定的 lagThreshold: 2，KEDA 就會立刻啟動 Pod。而在 K8s 內部，如果指標（Metrics）因為這種重設而卡住，HPA 就會一直維持在 3 個 Pod。

5. 要在程式裡面

 ack.acknowledge(); // 成功處理後手動提交

告訴 kafka 東西做完了

6. 分散式處理

KEDA 負責「廣度」： 看到 Lag 太高，就多開幾個 Pod（前提是有夠多的 Partition）。對應到 partition

Batch 負責「深度」： 讓單一 Pod 每次拉取（Poll）時，不是只拿 1 筆，而是拿一大捆（例如 100 筆）回來。
然後在開 Thread 去處理

會同時起幾個pod 決定在 kafka partition 數量 (參數 跟 new topic 都可以條)

k8s ScaledObject 裡面也有決定最大數量

kafka 空的時候 pod 為 0

